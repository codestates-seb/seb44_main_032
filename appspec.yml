version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/action/appspec.yml
    overwrite: yes

permissions:
  - object: /home/ubuntu/action/appspec.yml
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: server/scripts/deploy.sh
      timeout: 60
      runas: ubuntu
